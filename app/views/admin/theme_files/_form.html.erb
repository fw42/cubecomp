<%= form_for(theme_file_for_form(@theme_file)) do |f| %>
  <%= render 'form_errors', object: @theme_file, label: 'theme file' %>
  <%= render 'text_form', f: f %>

  <%= submit_button(f) %>
  <%= back_button(back_button_url) %>
<% end %>
