<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= @day.date.strftime("%A") %> - <%= current_competition.name %></title>
    <%= stylesheet_link_tag 'admin/schedule', media: 'all' %>
  </head>

  <body>
    <h1><%= current_competition.name %></h1>
    <%= yield %>
  </body>
</html>
